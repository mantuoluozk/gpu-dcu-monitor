# 更新记录

## 2026-07-20

- 新增第三个独立站点“郑州中心”，部署地址为 `10.211.5.22:3066`，首批纳管 `10.211.5.21`—`10.211.5.40` 共 20 台 BW1000 服务器；天津和郑州首页已提供三中心切换入口，太原入口待切换 VPN 后补充。
- 郑州部署机因内网 DNS 无法访问 apt 软件源，改为在本机下载并校验 Node.js v24.18.0 官方 SHA256 后离线安装；受监控机器密码仅用于首次建立公钥授权，不写入项目配置，`10.211.5.40` 虽然密码已变，但原有公钥仍可正常采集。
- 兼容郑州 BW1000 使用的 `hy-smi 1.24.0`：新增 `--showcunum` 查询以采集每张卡的真实 CU 数；CPU 温度新增 `k10temp` hwmon 和 BMC/IPMI 兜底，并在固件只返回通用 `Hygon C86 Processor` 时补充物理路数与每路核心数。
- 太原实例由已下架的 `10.20.100.19` 迁移到新服务器 `10.20.100.12`；使用本地保存的太原独立备份恢复 10 台服务器配置，并将站点访问地址更新为新地址。
- 新太原服务器按麒麟 V10 和 Node.js v12.22.11 环境重新建立 `/opt/gpu-dcu-monitor` 与 `gpu-dcu-monitor.service`，同时沿用管理员编辑锁配置；天津实例不受本次迁移影响。
- 同步更新天津实例首页的太原中心跳转链接，由旧地址 `10.20.100.19:3066` 改为新地址 `10.20.100.12:3066`；修改前已备份天津的 `data/sites.json`，未改动服务器列表和历史数据。
- 迁移时同步恢复新主机到太原受监控设备的 SSH 公钥授权；当前 6 台设备可正常采集，`10.20.100.18`、`10.20.100.19`、`10.20.100.15` 待补充 SSH 授权，`10.20.100.20` 待处理 DTK 25.04.2 与系统 `libstdc++` 的版本不匹配。

## 2026-07-17

- 统一首页顶部“管理员解锁”与“刷新状态”按钮的字号、字重、高度、圆角和水平内边距，仅保留权限状态配色差异，修复相邻操作文字规格不一致的问题。
- 修正历史存储测试的临时目录清理方式，避免使用 Node 14 才提供的 `fs.rmSync`，确保天津云端 Node.js v12.22.11 可以完整运行自动化测试。
- 新增单管理员编辑锁：所有访问者默认只读，只有输入管理员密码解锁后才显示服务器添加、编辑和删除入口；后端同步保护配置写接口，不能通过绕过前端直接修改。
- 管理员密码使用 scrypt 哈希环境变量配置，不写入 Git；登录后使用 8 小时 HttpOnly、SameSite 严格 Cookie，会话在服务重启时自动失效，并对连续错误密码进行限流。
- 新增 `npm run admin:hash` 密码哈希生成工具和管理员权限自动化测试；未配置管理员密码时监控、搜索、历史查看和刷新保持可用，但服务器信息始终只读。
- 历史使用记录新增整机 CPU 使用率：复用现有 10 秒采集结果，在一分钟历史桶中保存 CPU 平均值与峰值，并支持分钟数据、五分钟长期数据和旧历史文件兼容读取。
- 机器详情页历史模块新增 CPU 趋势折线和平均 CPU 汇总，CSV 同步增加 `cpu_utilization_avg`、`cpu_utilization_max` 字段；CPU 为整机指标，不受 GPU/DCU 单卡筛选影响。
- 修正历史压缩时缺失指标可能被当作 0 参与平均的问题，离线或探针未返回的 CPU、温度、功耗等数据继续保持为空档。
- 修正首页仅有 1 台或 2 台服务器时卡片被网格横向拉伸的问题：桌面宽度下恢复 372px 原始卡片宽度并保持左对齐，3 台及以上仍自动铺满三列；窄屏继续使用单列自适应布局。
- 移除首页搜索框内与占位文字重叠的装饰符号，恢复清晰的“搜索服务器、IP、分组、型号”输入提示。
- 参考内部模型服务平台的轻量视觉语言重构首页：改用白色基底、柔和环境渐变、轻边框和更清晰的标题层级，同时保留桌面三列、笔记本两列的监控效率。
- 顶部资源池与三项统计合并为统一数据面板，明确服务器使用“台”、加速卡使用“张”，避免统计口径混淆；搜索标签收入搜索框，减少工具栏拥挤。
- 首页服务器卡片减少大面积状态底色，改为局部状态光晕、徽标和占用环；提高硬件、卡槽、资产和心跳文字可读性。
- 设备区按卡数自适应：1 卡铺满一行、2 卡使用两列、3–8 卡保持紧凑网格，减少低卡服务器无意义的横向空白。
- 左侧导航统一使用“站点 / 状态 / 调度概况”中文标题，并将旧 DCU 方形标识替换为 GPU/DCU 通用环形品牌标记。

## 2026-07-16

- 提升详情页章节导航的字号、字重和颜色对比度，补充更完整的章节名称；历史区“导出 CSV”升级为高对比主按钮并增加下载方向图标与悬浮反馈。
- 参考内部模型服务平台的轻量视觉语言，对机器详情页进行结构级重构：新增平台式顶部导航、左右分栏机器 Hero、独立实时遥测面板、设备占用环和章节导航，大幅增加留白并减少同质化卡片堆叠。
- 重构机器详情页为硬件遥测工作台：强化机器身份区、实时状态指标、硬件分区层级和响应式布局，减少旧弹层的拥挤与视觉割裂。
- 历史趋势图新增纵坐标、单位、参考网格、时间轴、指标图例和悬浮十字线读数；算力/显存固定使用 0–100% 刻度，温度和功耗按数据自动生成易读量程。
- 新增每台 NVIDIA GPU / 海光 DCU 服务器的历史使用记录：保持 10 秒状态采集，在内存中按分钟聚合算力、显存、温度和功耗后落盘，不长期累积原始采样。
- 机器详情页新增历史汇总、算力/显存/温度/功耗趋势、整机与单卡切换，以及按时间范围导出 CSV 的入口。
- 历史数据近 90 天保留一分钟精度，更早数据自动聚合为五分钟并使用 gzip 压缩永久保存；离线分钟记录为空档，不按 0% 拉低利用率。
- 历史运行数据独立保存在 `data/history/` 并保持 Git 忽略；删除服务器配置不会同步删除历史，便于后续恢复和迁移。

## 2026-06-24

- 已将当前版本同步部署到昆山 / 天津实例 `10.8.145.247` 与太原实例 `10.20.100.19`；部署前已分别备份两台实例的 `data/servers.json` 和 `data/sites.json`，并通过 SHA256 校验确认更新过程未修改远端真实配置。
- 修复模型检索条件残留到资源池的问题：从“模型检索”切回“资源池”时会自动清空搜索框，并恢复显示全部符合状态/分组条件的服务器。
- 移除首页 GPU/DCU 标题右侧的温度、功耗、CU 三个采集完整度胶囊，诊断信息仅保留在机器详情页，减少首页视觉噪声。
- 重排首页 Hero 内 CPU 与 GPU/DCU 硬件摘要：类型标签固定在左侧，型号与规格统一在右侧两行对齐；同时提高 IP、说明文字、快速指标、设备格和资产底栏的字号、字重与颜色对比度。
- 明确区分 CPU 与加速卡遥测：CPU 温度缺失时显示“传感器未暴露”，不再使用含糊的“未接入”；详情页会标注温度来源，并把 hwmon 返回的整机读数标为“主机功耗”，避免误认为单颗 CPU 功耗。
- 机器详情页新增温度、功耗、CU 采集完整度提示，例如“温度 8/8、功耗 8/8、CU 0/8”；缺失项使用琥珀色提示，并说明目标驱动工具未返回相应数据。
- DCU CU 数继续坚持只展示驱动工具实际返回或已确认型号的可靠映射；NVIDIA GPU 明确标注 CU 不适用，不为 K100_AI、BW10 等 CU 配置可能不同的型号盲目推断。
- 修正紧凑卡片占用环在窄 Hero 中被遮挡的问题：占用环缩小到 58px，并扩大右侧 CPU/DCU 摘要列及其字号，弱化左侧说明文字。
- 压缩首页顶部区域：工具栏、标题、搜索框、资源池统计、状态统计和分组筛选统一降低高度；覆盖旧的 1180px 四行堆叠规则，笔记本宽度下保持单行或最多两行。
- 压缩 v2 首页机器卡片：将 CPU 与 GPU/DCU 硬件摘要移入占用主视觉区右侧，移除独立硬件行；设备总览改为 4 列 × 2 行，保留利用率、温度和负载条。
- 首页卡片改为 `minmax(300px, 1fr)` 自适应网格，在当前桌面内容区稳定排满 3 列，并同步缩小 Header、指标和 Footer 间距，减少单台机器占用面积。
- 接入用户提供的 `dcu v2` 前端方案：首页机器卡片改为独立 `.machine-card` 组件样式，严格采用 372px 宽度、18px 圆角、Inter/JetBrains Mono 字体栈、机器类型渐变图标和 v2 悬浮动效。
- 新增 88px SVG 占用环、busy/idle/alert/offline 四态视觉、温度阈值告警、每卡三行状态与显存条、彩色模型/镜像/运行时图标，以及基于当前各卡负载生成的趋势折线。
- v2 样式拆分到 `public/machine-card.css` 并通过命名空间隔离，避免历史首页卡片规则继续干扰；项目仍保持 React UMD 无构建运行，不引入 TypeScript 编译链或外部字体依赖。
- 按用户提供的单机首页 HTML demo 重构服务器卡片：采用独立标题栏、状态色主视觉区、三列快速指标、双硬件卡、4 列 × 2 行 GPU/DCU 设备详情和语义化资产底栏。
- GPU/DCU 卡槽改为每张卡显示利用率、温度及显存或功耗摘要，并通过运行/空闲状态点和红黄状态底色强化异常识别。
- 二次校准首页卡片以严格贴近 demo：统一使用其原始色板、字号、16px 圆角、轻边框、悬浮上移与阴影动效；GPU/DCU 改为 4 列 × 2 行，显著降低单卡高度。
- 根据逐项截图对比恢复 demo 原始的 2 列 × 4 行设备布局、CPU/加速卡图标、三项语义化底栏和心跳动画，并限制窄屏卡片最大宽度，避免被拉伸变形。
- 参考大尺寸硬件监控卡重新组织首页服务器卡片：新增更醒目的占用环、运行时间与心跳信息、带进度条的 CPU/内存/温度指标、硬件总线、加速卡利用率标题和更丰富的卡槽状态。
- 首页 GPU/DCU 卡槽增加利用率进度条、温度和功耗摘要，在保持三列服务器布局的同时提升信息可读性。
- 修复 `dmidecode` 每颗 CPU 明细中的 OPN 型号也可能被冒号截断的问题，首页和详情页统一保留完整具体型号。
- 重新设计首页服务器卡片的硬件信息区：CPU 使用冷蓝色铭牌，GPU/DCU 使用浅绿色设备区，卡槽和模型/镜像底栏也重新收口，提升信息层级并避免内容溢出。
- 修正首页硬件色块的设备类型判断，NVIDIA 服务器显示 GPU，海光服务器显示 DCU，不再受空闲/占用状态变量影响。
- CPU 型号在首页允许完整换行显示，并将 `Hygon C86-4G (OPN:7493)` 规范显示为 `Hygon C86-4G · OPN 7493`。
- 修复 CPU 具体型号采集在 `OPN:7493` 处被截断的问题：`lscpu` 和 `/proc/cpuinfo` 解析现在只移除字段名前的第一个冒号，不再破坏型号内部内容。
- 补充本地项目记忆，记录 Windows/Codex 环境下可靠的后台启动、端口进程清理、首页与 API 双重验证流程，以及 CPU 型号解析注意事项。

## 2026-06-23

- 首页服务器卡片重新排版为更紧凑的监控面板，系统摘要、DCU 小格、模型/镜像统计会稳定收在卡片内部。
- 缺少 DCU CU 数或型号缓存的机器会在普通状态刷新时自动补采一次拓扑信息，BW100/BW150/BW1000 等明确型号在目标机缺少 `rocminfo` 时也会按型号兜底显示 CU。
- 修复 DCU 详情页被旧 `gpuCount` 撑出不存在卡位的问题：状态刷新现在以本轮真实采集到的卡数为准，CU 数也会随型号信息持久化。
- 首页服务器卡片新增系统身份摘要，显示 CPU 型号、系统版本和 GPU/DCU 驱动版本，方便不打开详情也能快速识别机器环境。
- CPU 信息新增具体型号/部件信息展示，优先读取 `dmidecode -t processor`，用于识别 7470 等更细型号；取不到时回退到系统通用型号。
- CPU 板块新增物理 CPU 翻页卡片，可逐颗查看 Socket、具体型号、部件号、核心/线程数和频率信息；负载显示改为标注 1m/5m/15m。
- CPU 翻页改为仅在每颗 CPU 明细确实不同时显示；同型号多路机器只展示汇总，避免重复页面。CPU 具体型号优先采用 `lscpu` 中带 OPN 的 `Model name`。
- DCU 卡详情新增 CU 数展示，优先从 `hy-smi -q` 解析，必要时尝试 `rocminfo` 作为兜底。
- 修正 `rocminfo` CU 解析：会跳过 Hygon CPU Agent，只将 `gfx*` / `BW*` / `C-3000` 等 DCU Agent 映射到 DCU 卡。
- `rocminfo` 采集增加登录 Shell 和 `/opt/hyhal/env.sh` 兜底；`hy-smi -q` 的 CU 行在没有卡号时会按出现顺序映射到 DCU 卡。
- `rocminfo` 解析改为后端 Node.js 解析关键行，避免远端 shell/awk 引号差异导致 CU 数无法返回。
- 详情页视觉分区增加主题色标题条，CPU、内存、系统、DCU/GPU、模型镜像板块使用不同强调色，提升扫描辨识度。
- 修复详情页点击“刷新当前”后白屏的问题：单台刷新结果现在会正确合并回服务器列表，并保留已加载的模型/镜像详情。
- CPU 型号汇总改为按物理 CPU/socket 统计，不再把每个逻辑核心都算成一颗 CPU；详情页同时显示物理 CPU 数和逻辑核心数。
- 重构服务器详情页，将信息拆分为 CPU、内存、系统版本、DCU/GPU 卡状态、模型与镜像几个板块，减少系统和驱动信息挤在一起的问题。
- 新增单台服务器刷新入口：详情页可以只刷新当前机器的 DCU/GPU、CPU、内存、系统和驱动信息，不再必须触发全局刷新。
- 优化 CPU 与系统探针：CPU 型号支持多型号/多路汇总展示，驱动版本过滤会避开 `hy-smi` 横线和 banner 文本，并兼容纯版本号输出。
- CPU 功耗增加 `powercap`、`hwmon power*_input` 和 `sensors` 多层兜底；如果目标服务器没有暴露功耗传感器，界面仍会显示为空。

## 2026-06-22

- 新增 CPU 与系统信息采集：服务器刷新时会同步采集 CPU 利用率、核心数、CPU 型号、内存使用率、负载、运行时间、系统版本、内核架构和 GPU/DCU 驱动版本。
- 新增 CPU 温度和 CPU 功耗 best-effort 展示：优先读取 `sensors`、`/sys/class/thermal` 和 `powercap energy_uj`，无法读取时在界面显示为空，不影响 GPU/DCU 占用状态刷新。
- 前端服务器卡片新增 CPU、内存和温度摘要；服务器详情页新增“CPU 与系统信息”面板，集中展示系统版本、驱动版本、CPU 型号、功耗、温度和运行时间。
- 新增 `SYSTEM_SSH_TIMEOUT_MS` 环境变量控制系统探针超时，默认不超过 12 秒；系统探针失败不会把服务器判定为离线。

## 2026-06-15

- 将模型资产默认扫描深度从 12 层调整为 6 层，减少对深目录和慢挂载盘的无效扫描压力；仍可通过 `ASSET_SCAN_MAX_DEPTH` 按部署环境单独调大。
- 收窄模型资产默认扫描路径，移除 `/opt`、`/tpstor`、`/glusterfs-user-data`、`/Ring-2.5-1T` 等偏固定环境的目录；需要时可通过 `ASSET_PATHS` 单独配置。
- 重构前端看板视觉为更紧凑的资源调度台风格，将全局刷新、模型资产刷新和添加服务器移到顶部操作区，服务器卡片改为更清晰的卡位矩阵展示。
- 将前端升级为 React 组件化实现，并引入本地 React 运行时文件；页面改为“机柜调度室”风格，强化资源池总览、GPU/DCU 槽位灯阵、模型检索和右侧检查面板。
- 优化 React 看板首页的信息密度：移除常驻右侧详情列，点击服务器后再打开详情覆盖页；收窄左侧筛选栏、压缩服务器卡片高度，并将配色调整为更清爽的石墨蓝冷白风格。
- 修复服务器卡片底部标签在紧凑布局下被遮挡的问题，首页仅展示关键标签并用数量徽标汇总其余标签。
- 进一步简化首页服务器卡片，移除底部标签行并将编辑入口移到右上角，避免模型数、镜像数和盘点时间在紧凑布局下溢出卡片。
- 恢复首页服务器卡片的模型数、镜像数和盘点时间摘要，并放大卡片宽度与高度下限，确保资产摘要在卡片内完整显示。
- 隐藏顶部“刷新模型资产”手动入口，保留每日自动盘点和模型镜像检索能力，减少误触发深目录扫描的风险。
- 补充本地项目记忆中的双云端部署信息，记录昆山 / 天津与太原两个实例的访问地址、部署目录和部署注意事项；同时清理本地日志、旧部署包等运行产物。

## 2026-06-12

- 补充 Windows 本地调试说明：PowerShell 可能因执行策略拦截 `npm.ps1`，建议使用 `cmd.exe /c npm.cmd start`、`start-windows.bat` 或直接 `node server.js` 启动。
- 记录本机 Codex/Windows 环境访问差异：`localhost:3066` 可能因 IPv6 解析超时，遇到页面打不开时优先使用 `http://127.0.0.1:3066/`。
- 模型资产结果过滤框架和工程目录，例如 `vllm`、`LLaMA-Factory`、`stable-diffusion-webui`、`model-downloader-main`，避免把运行框架误当作模型展示。
- 修复模型路径详情滚动时偶尔跳动的问题，自动轮询刷新时会保留已加载的模型详情，并在重绘后恢复右侧详情和模型列表的滚动位置。

## 2026-06-11

- 保留“刷新模型资产”手动入口，方便本地测试和上线后立即盘点；同时模型路径盘点会每天 02:00 自动执行。
- 扩大模型资产扫描范围，默认覆盖 `/models`、`/model`、`/public`、`/data`、`/mnt`、`/home`、`/root`、`/workspace`、`/workspaces`、`/opt` 等常见目录，并识别更多权重、配置和推理引擎文件。
- 模型资产默认返回上限提升到 800 条，并支持通过 `ASSET_SCAN_MAX_DEPTH`、`ASSET_REFRESH_HOUR`、`ASSET_REFRESH_MINUTE` 调整扫描深度和自动盘点时间。
- 根据太原送测设备模型统计表补全 `/data1`、`/data2`、`/data_nvme*`、`/apps`、`/aidata`、`/storage`、`/tpstor`、`/glusterfs-user-data` 等模型落点，默认扫描深度提升到 12 层，条目上限提升到 1500 条。
- 收紧模型资产筛选：权重和推理引擎文件直接展示；`config.json`、`tokenizer.json` 等弱特征只用于确认模型目录，不再单独作为模型文件展示，减少普通项目目录混入。
- 单台服务器模型资产盘点默认超时从 30 秒提升到 120 秒，减少深目录扫描时被提前判定为盘点失败。
- 模型资产盘点改为按根目录独立限时扫描，单个大目录或慢挂载超时后会跳过该目录，尽量保留同一机器其他目录的盘点结果。
- 模型资产列表改为按模型目录聚合展示，不再把 `.gguf`、`.safetensors`、`.bin` 等权重文件逐条铺在界面上；这些文件名仍会参与搜索命中。
- 新增“更新日志”页面，用户可以在界面中直接查看每次功能变化和部署兼容性说明。

## 2026-06-04

- 准备太原中心本地服务器配置，整理为 10 台太原机器，按太原机柜位置和组织归属分组，并保持标签只包含位置、机型和序列号。
- 将天津 / 昆山公共服务的本地服务器配置整理为 18 台机器，按组织或实验室分组，并清理标签中的借用人、用途和状态信息。
- 增加 `hy-smi` 采集兜底：当默认环境和登录 Shell 都找不到命令时，会尝试先加载 `/opt/hyhal/env.sh`，兼容需要手动 source HYHAL 环境的机器。

## 2026-05-29

- 新增多站点入口切换，页面侧边栏可以在昆山 / 天津中心、太原中心等独立部署之间跳转。
- 新增 `/api/site-config` 配置接口，支持通过 `data/sites.json` 或环境变量维护当前站点和站点列表。
- 新增 `data/sites.sample.json` 示例配置，并将真实站点入口 `data/sites.json` 作为本地运行配置忽略，避免提交真实内网地址。

## 2026-05-28

- 新增“模型镜像检索”视图，支持按模型名、路径、Docker 镜像名或 tag 查找资产所在服务器。
- 检索结果按服务器聚合展示，并显示机器分组、IP、当前占用状态，支持复制 IP、SSH 命令、模型路径和镜像名称。
- 优化资源看板和模型检索的顶部工具栏，让搜索框明确跟随当前视图显示资源搜索或模型搜索。
- 收紧模型目录识别规则，只展示目录名或目录内容具备模型特征的路径，减少项目目录、脚本目录等非模型内容混入。
- 新增服务器配置定期备份，默认保存到 `data/backups/` 并保留最近 30 份。
- 新增模型资产盘点，支持展示每台服务器 `/models`、`/public`、`/data` 等目录下的模型文件/目录和 Docker images。
- 新增“刷新模型资产”入口，并支持按模型路径、模型名和镜像名称进行搜索。
- 新增采集并发控制，GPU/DCU 状态默认最多同时采集 8 台，模型资产默认最多同时采集 3 台。
- 新增 `hy-smi` 登录 Shell 兜底采集方式，兼容需要加载启动脚本后才能使用 DTK 环境的服务器。
- 验证本地可以访问 `10.17.26.107`，并把同样的采集方式同步到公共部署服务。

## 2026-05-27

- 优化宽屏下的服务器卡片布局，修复卡片内容超出边界的问题。
- 将默认 SSH/采集超时时间调整为 20 秒，减少 NVIDIA `nvidia-smi` 响应较慢时误判连接失败的情况。
- 新增 GPU/DCU 型号持久化，并改为添加、编辑或手动刷新时更新型号信息。
- 新增服务器分组和分组筛选功能。
- 更新 README，补充部署说明和界面截图。
